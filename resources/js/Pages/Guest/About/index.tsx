// resources/js/Pages/About.tsx
import React from 'react';

// In React Native, you won't use <h1> or <> (fragments) the same way,
// but the component structure remains identical.
const About = () => {
    return (
        <div>
            <h1>Duka - About</h1>
        </div>
    );
}

export default About;